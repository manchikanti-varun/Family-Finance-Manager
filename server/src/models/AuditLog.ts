import { Schema, model, Types } from 'mongoose';
export const AuditLog=model('AuditLog',new Schema({workspaceId:{type:Types.ObjectId,required:true,index:true},userId:{type:Types.ObjectId,required:true},action:{type:String,required:true},entityType:{type:String,required:true},entityId:{type:Types.ObjectId,required:true},oldValue:Schema.Types.Mixed,newValue:Schema.Types.Mixed},{timestamps:{createdAt:true,updatedAt:false},strict:'throw'}));
